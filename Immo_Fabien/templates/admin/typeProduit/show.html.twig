{% extends 'base.html.twig' %}

{% block title %}TypeProduit{% endblock %}

{% block body %}
    <h1>TypeProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr>
            <tr>
                <th>NomTypeProduit</th>
                <td>{{ type_produit.nomTypeProduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_produit_index') }}" class="btn btn-secondary">Retour</a>

    <a href="{{ path('type_produit_edit', {'id': type_produit.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('admin/typeProduit/_delete_form.html.twig') }}
{% endblock %}
